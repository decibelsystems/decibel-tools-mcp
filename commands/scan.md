Scan the current project for issues.

Use the `sentinel_scan` tool with:
- projectId: auto-detect from current directory
- scope: "data"
- flags: ["orphans", "stale", "invalid"]

Report findings in a concise summary, highlighting any critical issues first.
