Initialize Decibel in the current project.

Use the `project_init` tool with path set to the current working directory. If the project already has a .decibel folder, inform me and ask if I want to reinitialize with force=true.
