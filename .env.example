# Decibel MCP Server Configuration

# Environment (dev, staging, prod)
DECIBEL_ENV=dev

# Organization name
DECIBEL_ORG=mediareason

# Root directory for storing global data files (optional)
# Default: ~/.decibel
# Project-local data is stored in each project's .decibel/ folder
DECIBEL_MCP_ROOT=/path/to/your/global/decibel/data
