# Decibel MCP Server Configuration

# Environment (dev, staging, prod)
DECIBEL_ENV=dev

# Organization name
DECIBEL_ORG=mediareason

# Root directory for storing data files
# Default: ./data under the project root
DECIBEL_MCP_ROOT=/path/to/your/decibel-mcp-data
